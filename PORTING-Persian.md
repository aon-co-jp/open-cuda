# open-cuda — راهنمای انتقال (نسخهٔ فشرده)

> **یادداشت**: این ترجمه‌ای فشرده از الگوهای قابل استفادهٔ مجدد فعلی
> است. گزارش تاریخی مفصل HANDOFF تنها به زبان ژاپنی در
> [PORTING.md](PORTING.md) در دسترس باقی می‌ماند — پیش از پیاده‌سازی
> واقعی هر الگو باید به آن مراجعه شود.

خلاصه‌ای از الگوهای طراحی قابل استفادهٔ مجدد این پروژه، برای زمانی
که بخواهند به پروژه‌ای دیگر منتقل شوند.

1. **تریت `GpuDevice` (طراحی مستقل از بک‌اند)**: قراردادی حداقلی در
   سبک CUDA Runtime API (‏`alloc`/`free`/`memcpy_h2d`/`memcpy_d2h`/
   `memcpy_d2d`/`launch_kernel`/`synchronize`) به‌همراه پرچم‌های
   قابلیت (‏`supports_spirv`/`supports_dxil`، پیش‌فرض `false`).
   بک‌اندهای سخت‌افزاری جدید این قرارداد را پیاده‌سازی می‌کنند و
   enum ‏`KernelSource` را به‌شکلی غیرمخرب گسترش می‌دهند (مثلاً
   `Dxil(Vec<u8>)`).
2. **الگوی دومرحله‌ای «مدل ساختگی ← سخت‌افزار واقعی»**: مرحلهٔ ۱
   (دستگاه ساختگی، بدون نیاز به سخت‌افزار اجرا می‌شود، قراردادها را
   حتی در CI بدون GPU تأیید می‌کند)، مرحلهٔ ۱.۵ تا ۲ (پیاده‌سازی
   واقعی پشت یک ویژگی Cargo، به‌طور پیش‌فرض غیرفعال، در نبود
   سخت‌افزار واقعی به‌شکلی صادقانه از خود صرف‌نظر می‌کند).
3. **تلهٔ padding آرایه‌ها در cbuffer زبان HLSL**: آرایه‌های اسکالر
   درون `cbuffer` تا مرز ۱۶ بایتی padding می‌شوند — ثابت‌های
   فشرده‌شدهٔ Rust/C++ دیگر با چیدمان HLSL هم‌خوانی ندارند (در عمل
   باعث باگ «خروجی هم‌چنان متن ساده باقی می‌ماند» در کرنل ChaCha20
   شد). راه‌حل: فیلدهای اسکالر جداگانه (‏`key0`…`key7`) به‌جای
   اعلان آرایه.
4. **امضای ریشه (Root Signature) تعبیه‌شده در HLSL**: ویژگی
   `[RootSignature(...)]` که مستقیماً در شیدر نوشته می‌شود باعث
   می‌شود `dxc` امضای ریشه را در DXIL جای دهد — از ساخت دستی توصیفگر
   امضای ریشه در کد Rust/C++ جلوگیری می‌کند. اتصال مستقیم توصیفگرهای
   UAV ریشه به‌جای عبور از descriptor heap.
5. **شمارش آداپتورهای DXGI برای تشخیص سازنده**: `D3D12CreateDevice
   (None, ...)` اطلاعاتی دربارهٔ سازنده یا ظرفیت VRAM نمی‌دهد.
   `IDXGIFactory1::EnumAdapters1(0)` ← `DXGI_ADAPTER_DESC1` شناسهٔ
   سازندهٔ PCIe را می‌دهد (NVIDIA/AMD/Intel). در صورت شکست، بازگشت
   ایمن به `None`.
6. **هشدار صادقانه دربارهٔ فشرده‌سازی/رمزگذاری روی GPU**: برای
   payload‌های کوچک (اندازهٔ MTU شبکه) سربار انتقال Host↔Device
   ممکن است مزیت محاسباتی GPU را خنثی کند — پیش از یکپارچه‌سازی باید
   محک‌های واقعی روی اندازهٔ payload هدف اجرا شود.
7. **الگوی کرنل‌های توازن RAID6**: تعداد متغیر دیسک‌های داده به‌صورت
   یک بافر به‌هم‌پیوسته (به‌جای پیوندهای جداگانه برای هر دیسک). ضرب
   در GF(2^8) برای Q-parity (‏Reed-Solomon) به‌صورت تابعی مستقل
   `gf_mul` (ضرب به سبک «دهقان روسی»، چندجمله‌ای تحویل‌ناپذیر
   `0x11D`) درون شیدر.
8. **پیاده‌سازی GPU مستقل از اعداد صحیح ۶۴ بیتی**: اعداد صحیح ۶۴
   بیتی یک قابلیت اختیاری سخت‌افزاری در DXIL SM6.0 هستند. Poly1305
   ضرب/جمع/شیفت جفتی ۳۲×۳۲←۶۴ بیت را تنها با عملیات ۳۲ بیتی
   پیاده‌سازی می‌کند — الگویی مفید برای انتقال رمزنگاری/اعداد بزرگ به
   هدف‌هایی بدون تضمین ۶۴ بیت.
9. **فشرده‌سازی حافظهٔ نهان KV به سبک MLA در DeepSeek-V3**: تصویرسازی
   کاهشی/افزایشی بر پایهٔ `sgemm` موجود. **افشای صادقانه**:
   ماتریس‌های تصویرسازی تنها با مقداردهی تصادفی اولیه شده‌اند (بدون
   وزن‌های آموزش‌دیده) — فشرده‌سازی با اتلاف است؛ این تنها نشان
   می‌دهد که مسیر محاسباتی به‌درستی سیم‌کشی شده است، نه اینکه کیفیت
   تولید حفظ می‌شود.
10. **جریمهٔ تکرار (به سبک CTRL)**: `logit>0` ← `/penalty`،
    `logit<=0` ← `*penalty`، پیش از argmax روی توکن‌های پیش‌تر
    دیده‌شده اعمال می‌شود. `penalty=1.0` مسیری میان‌بر است که رفتار
    موجود `generate()` را دقیقاً حفظ می‌کند (بدون افت عملکرد). مقدار
    پیش‌فرض تجربی `1.3` — بسته به ساختار prompt باید دوباره کالیبره
    شود.

**وضعیت فعلی**: یک workspace از Cargo متشکل از `opencuda-core`/
`opencuda-cpu`/`opencuda-vulkan`/`opencuda-directx`/`opencuda-blas`/
`open-cuda-bert`/`open-cuda-llm`. `opencuda-directx` تا مرحلهٔ ۲
پیاده‌سازی شده (‏vector_add/matmul/ChaCha20/Poly1305 روی سخت‌افزار
واقعی تأیید شده‌اند). کرنل‌های توازن P و Q در RAID6 به
`opencuda-vulkan` افزوده و روی سخت‌افزار واقعی تأیید شده‌اند. جزئیات
در ورودی‌های HANDOFF فایل [CLAUDE.md](CLAUDE.md).
