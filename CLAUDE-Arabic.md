# فلسفة التصميم وسياسة التطوير (open-cuda)

> **ملاحظة**: هذه ترجمة مختصرة للحالة الحالية. يظل سجل التغييرات
> التاريخي الكامل HANDOFF (عشرات الإدخالات منذ 2026-06-26) متاحًا
> باللغة اليابانية فقط في [CLAUDE.md](CLAUDE.md) توخيًا للإيجاز — راجعه
> للاطلاع على تفاصيل كل جلسة.

قرص العمل: `F:\runo`. مستودع GitHub:
[aon-co-jp/open-cuda](https://github.com/aon-co-jp/open-cuda). بداية
التطوير: 2026-06-26.

## دور هذا المشروع

"CUDA الثاني" — أساس لتجريد/حوسبة وحدة معالجة الرسومات (تصميم
`OmniGPU`، راجع `OmniGPU-Design.md`) يهدف إلى التوافق مع
Windows/macOS/Linux و Intel/AMD/NVIDIA. يشكّل "SET" مع `aruaru-llm`،
وهو المستهلك الفعلي لخط أنابيب تنفيذ GPU/CPU.

## بنية الحزم (crates)

- **`opencuda-core`**: السمة (trait) المشتركة `GpuDevice` (مكافئ CUDA
  Runtime API).
- **`opencuda-cpu`**: الواجهة الخلفية للمعالج (توازي البيانات عبر
  `rayon`).
- **`opencuda-vulkan`**: الواجهة الخلفية Vulkan Compute العابرة
  للمنصات (أصلية على Windows/Linux/Android، وmacOS/iOS عبر MoltenVK).
  تم التحقق من GEMM/Attention/تكميم INT4·INT8 على عتاد حقيقي.
- **`opencuda-directx`**: واجهة خلفية DirectX 12 Compute (خاصة بـ
  Windows فقط، تتعايش مع Vulkan). تم التحقق من إرسال نوى vector_add/
  matmul/ChaCha20/Poly1305 على عتاد حقيقي (GT 730)؛ تم تنفيذ تعداد
  مهايئات DXGI لاكتشاف المورّد/VRAM.
- **`opencuda-blas`**: مكافئ NumPy (GEMM/Attention/تكميم/
  Flash Attention/ضغط KV من نوع MLA).
- **`open-cuda-bert`**: المرور الأمامي لمشفّرات BERT
  (multilingual-e5-small).
- **`open-cuda-llm`**: مكافئ vLLM — مفكّك ترميز GPT-2 بذاكرة تخزين KV،
  عقوبة التكرار، ضغط MLA، flash attention و(أحدث ميزة) فك الترميز
  التخميني (speculative).
- **`open-cuda-whisper`**: مكافئ Whisper (log-mel-spectrogram + مشفّر +
  مفكّك ترميز بذاكرة تخزين KV + cross-attention)، حاليًا مجرد MVP
  بتهيئة عشوائية فقط.

## الحالة الصادقة للإفصاح

- **cuBLAS/rocBLAS/oneMKL تظل نائبة (stubs) غير مُتحقَّق منها** — لا
  يمتلك هذا الجهاز سلسلة أدوات CUDA/ROCm/oneAPI للتحقق.
- **تم رفض FP8**: لا تملك وحدة معالجة الرسومات الوحيدة لهذا الجهاز
  (GT730، Kepler، CC 3.5) أنوية Tensor لـ FP8 — سيكون التنفيذ مجرد
  محاكاة برمجية دون فائدة حقيقية.
- **تم رفض DeepSeekMoE**: يحتوي `DecoderLayer` على FFN كثيف واحد فقط
  (بدون بنية خبراء/موجّه)، ولا توجد نقطة تفتيش MoE حقيقية.
- **الضغط/التشفير على GPU**: الفائدة الحقيقية للحمولات الصغيرة غير
  مؤكدة (قد تُلغي النفقات العامة بين المضيف والجهاز ميزة GPU).
- **اكتشاف على جهاز Android حقيقي (2026-08-15)**: على جهاز Android
  (moto g53y 5G، Adreno 619)، تفوقت وحدة معالجة الرسومات في الهاتف
  على كل من المعالج ومقارنة CPU/GPU الخاصة بـ GT730 لسطح المكتب عند
  أحجام مصفوفات أكبر (حتى ~6 أضعاف عند 512×512)، مما أكد فرضية أن
  "وحدات معالجة الرسومات في الهواتف قد تكون سريعة بشكل مفاجئ". تحفظ:
  لا تزال الحسابات الصغيرة الفردية مقيدة بالنفقات العامة لتهيئة GPU
  (58–63 مللي ثانية).

## مصفوفة دعم الموردين

ثلاث طبقات: تكامل Vulkan العامل / تعداد `GpuVendor` كطبقة إبلاغ فقط
(بما في ذلك Qualcomm/ARM/ImaginationPowerVr غير المُتحقَّق منها) /
طبقة نائبة لمكتبات الموردين. التفاصيل في `OmniGPU-Design.md` القسم
8.5.

## إدخالات HANDOFF الأخيرة ذات الصلة

- **2026-08-10**: تعالج `generate_with_repetition_penalty` (عقوبة
  التكرار على طراز CTRL) خللًا حقيقيًا في الحلقة اللانهائية في فك
  الترميز الجشع لـ GPT-2 في `aruaru-llm`. القيمة الافتراضية
  `penalty=1.3` عبر `ARUARU_LLM_REPETITION_PENALTY`.
- **أحدث عمل (الالتزام `0c43ba3`)**: دالة جديدة
  `GptModel::generate_speculative` — فك ترميز تخميني بلا فقدان على
  طراز DSpark/Leviathan (نموذج المسودة يقترح رموزًا، والنموذج
  المستهدف يتحقق عبر تعبئة مسبقة دفعية). تم التحقق من كونه مطابقًا
  بت تلو الآخر لـ `generate()` على تجهيزات اختبار اصطناعية وأوزان
  حقيقية. **إفصاح صادق**: على مسار المعالج لهذا الجهاز، أبطأ من
  `generate()` البسيط (draft_k=4: العادي 4.63 ثانية مقابل التخميني
  7.65 ثانية)، لأن GEMM الساذج على المعالج لديه القليل من النفقات
  العامة للإرسال لاستهلاكها. الحالة المستهدفة الحقيقية — السرعة على
  Vulkan حقيقي — لا تزال غير مقاسة.
