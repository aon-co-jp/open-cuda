# open-cuda — מדריך העברה (גרסה מקוצרת)

> **הערה**: זהו תרגום מקוצר של הדפוסים הניתנים לשימוש חוזר כיום.
> יומן ה-HANDOFF ההיסטורי המפורט נשאר זמין רק ביפנית בקובץ
> [PORTING.md](PORTING.md) — יש לעיין בו לפני אימוץ בפועל של דפוס
> כלשהו.

סיכום דפוסי העיצוב הניתנים לשימוש חוזר בפרויקט זה, לשימוש בעת העברה
לפרויקט אחר.

1. **הטרייט `GpuDevice` (עיצוב בלתי תלוי בבאקאנד)**: חוזה מינימלי
   בסגנון CUDA Runtime API (‏`alloc`/`free`/`memcpy_h2d`/
   `memcpy_d2h`/`memcpy_d2d`/`launch_kernel`/`synchronize`) בתוספת
   דגלי יכולת (‏`supports_spirv`/`supports_dxil`, ברירת מחדל
   `false`). באקאנדים חדשים של חומרה מממשים חוזה זה ומרחיבים את
   ה-enum ‏`KernelSource` באופן שאינו שובר תאימות (למשל
   `Dxil(Vec<u8>)`).
2. **דפוס דו-שלבי "מוק ← חומרה אמיתית"**: שלב 1 (התקן מדומה, פועל
   ללא חומרה, מאמת חוזים גם בסביבות CI ללא GPU), שלב 1.5–2 (מימוש
   אמיתי מאחורי feature של Cargo, כבוי כברירת מחדל, מדלג על עצמו
   ביושר כשאין חומרה אמיתית).
3. **מלכודת ריפוד המערכים ב-cbuffer של HLSL**: מערכים סקלריים בתוך
   `cbuffer` מרופדים לגבול של 16 בייט — קבועים ארוזים בצפיפות מצד
   Rust/C++ אינם תואמים עוד את הפריסה ב-HLSL (גרם בפועל לבאג
   "הפלט נשאר טקסט גלוי" בקרנל ChaCha20). הפתרון: שדות סקלריים
   נפרדים (‏`key0`…`key7`) במקום הצהרת מערך.
4. **חתימת שורש משובצת ב-HLSL**: התכונה `[RootSignature(...)]`
   הכתובה ישירות בשיידר גורמת ל-`dxc` לשבץ את חתימת השורש לתוך
   ה-DXIL — חוסך בניית תיאור חתימת שורש ידני בקוד Rust/C++. קישור
   ישיר של תיאורי UAV שורשיים במקום דרך descriptor heap.
5. **מנייה של מתאמי DXGI לזיהוי היצרן**: `D3D12CreateDevice(None,
   ...)` אינו מספק מידע על יצרן או נפח VRAM. `IDXGIFactory1::
   EnumAdapters1(0)` ← `DXGI_ADAPTER_DESC1` מספק מזהה יצרן PCIe
   (NVIDIA/AMD/Intel). בעת כשל יש לחזור בבטחה ל-`None`.
6. **אזהרה כנה בנוגע לדחיסה/הצפנה על ה-GPU**: עבור מטענים קטנים
   (בגודל MTU רשתי) תקורת ההעברה Host↔Device עלולה לבטל את היתרון
   החישובי של ה-GPU — יש להריץ מדידות ביצועים אמיתיות בגודל המטען
   היעד לפני אינטגרציה.
7. **דפוס קרנלי זוגיות RAID6**: מספר משתנה של דיסקי נתונים כמאגר
   מחובר יחיד (במקום קישורים נפרדים לכל דיסק). כפל ב-GF(2^8) עבור
   Q-parity (‏Reed-Solomon) כפונקציה עצמאית `gf_mul` (כפל בסגנון
   "האיכר הרוסי", פולינום בלתי פריק `0x11D`) בתוך השיידר.
8. **מימוש GPU שאינו תלוי במספרים שלמים של 64 סיביות**: מספרים
   שלמים של 64 סיביות הם תכונת חומרה אופציונלית ב-DXIL SM6.0.
   Poly1305 מממש כפל/חיבור/הזזה זוגיים 32×32←64 סיביות תוך שימוש
   בפעולות 32 סיביות בלבד — דפוס שימושי להעברת קריפטוגרפיה/מספרים
   גדולים ליעדים ללא ערבות ל-64 סיביות.
9. **דחיסת מטמון KV בסגנון MLA של DeepSeek-V3**: הטלה יורדת/עולה
   הבנויה על ה-`sgemm` הקיים. **גילוי כנה**: מטריצות ההטלה
   מאותחלות אקראית בלבד (ללא משקלים מאומנים) — הדחיסה היא עם אובדן
   מידע; הדבר מוכיח רק שהנתיב החישובי מחווט כראוי, לא ששמורה איכות
   הייצור.
10. **קנס חזרתיות (בסגנון CTRL)**: `logit>0` ← `/penalty`,
    `logit<=0` ← `*penalty`, מוחל על טוקנים שכבר נראו לפני
    ה-argmax. `penalty=1.0` הוא נתיב מקוצר השומר בדיוק על ההתנהגות
    הקיימת של `generate()` (ללא רגרסיה). ערך ברירת מחדל אמפירי
    `1.3` — דורש כיול מחדש בהתאם למבנה ה-prompt.

**מצב נוכחי**: workspace של Cargo המורכב מ-`opencuda-core`/
`opencuda-cpu`/`opencuda-vulkan`/`opencuda-directx`/`opencuda-blas`/
`open-cuda-bert`/`open-cuda-llm`. `opencuda-directx` ממומש עד שלב 2
(‏vector_add/matmul/ChaCha20/Poly1305 אומתו על חומרה אמיתית). קרנלי
זוגיות RAID6 מסוג P/Q נוספו ל-`opencuda-vulkan` ואומתו על חומרה
אמיתית. פרטים ברשומות ה-HANDOFF בקובץ [CLAUDE.md](CLAUDE.md).
