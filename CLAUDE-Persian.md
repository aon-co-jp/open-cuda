# فلسفه طراحی و سیاست توسعه (open-cuda)

> **یادداشت**: این ترجمه‌ای فشرده از وضعیت فعلی است. گزارش کامل تاریخی
> تغییرات HANDOFF (ده‌ها ورودی از 2026-06-26) برای اختصار فقط به زبان
> ژاپنی در [CLAUDE.md](CLAUDE.md) در دسترس باقی می‌ماند — برای جزئیات
> هر جلسه به آنجا مراجعه کنید.

درایو کاری: `F:\runo`. مخزن GitHub:
[aon-co-jp/open-cuda](https://github.com/aon-co-jp/open-cuda). شروع
توسعه: 2026-06-26.

## نقش این پروژه

«دومین CUDA» — یک بنیاد انتزاع/محاسبات GPU (طراحی `OmniGPU`، به
`OmniGPU-Design.md` مراجعه کنید) که هدف آن سازگاری با
Windows/macOS/Linux و Intel/AMD/NVIDIA است. با `aruaru-llm`، که
مصرف‌کننده واقعی خط لوله اجرای GPU/CPU است، یک «SET» تشکیل می‌دهد.

## معماری crate ها

- **`opencuda-core`**: صفت (trait) مشترک `GpuDevice` (معادل CUDA
  Runtime API).
- **`opencuda-cpu`**: بک‌اند CPU (موازی‌سازی داده از طریق `rayon`).
- **`opencuda-vulkan`**: بک‌اند Vulkan Compute چندسکویی (بومی روی
  Windows/Linux/Android، macOS/iOS از طریق MoltenVK). GEMM/Attention/
  کوانتیزاسیون INT4·INT8 روی سخت‌افزار واقعی تأیید شده‌اند.
- **`opencuda-directx`**: بک‌اند DirectX 12 Compute (فقط Windows،
  هم‌زمان با Vulkan وجود دارد). ارسال هسته‌های vector_add/matmul/
  ChaCha20/Poly1305 روی سخت‌افزار واقعی (GT 730) تأیید شده؛ شمارش
  آداپتورهای DXGI برای تشخیص سازنده/VRAM پیاده‌سازی شده است.
- **`opencuda-blas`**: معادل NumPy (GEMM/Attention/کوانتیزاسیون/
  Flash Attention/فشرده‌سازی KV نوع MLA).
- **`open-cuda-bert`**: گذر رو به جلو برای رمزگذارهای BERT
  (multilingual-e5-small).
- **`open-cuda-llm`**: معادل vLLM — رمزگشای GPT-2 با کش KV، جریمه
  تکرار، فشرده‌سازی MLA، flash attention و (جدیدترین ویژگی) رمزگشایی
  حدسی (speculative).
- **`open-cuda-whisper`**: معادل Whisper (log-mel-spectrogram + رمزگذار
  + رمزگشا با کش KV + cross-attention)، در حال حاضر فقط یک MVP با
  مقداردهی اولیه تصادفی.

## وضعیت صادقانه افشا

- **cuBLAS/rocBLAS/oneMKL همچنان stub تأییدنشده باقی می‌مانند** — این
  دستگاه ابزار زنجیره CUDA/ROCm/oneAPI برای تأیید ندارد.
- **FP8 رد شد**: تنها GPU این دستگاه (GT730، Kepler، CC 3.5) هسته‌های
  Tensor مخصوص FP8 ندارد — پیاده‌سازی صرفاً شبیه‌سازی نرم‌افزاری بدون
  سود واقعی خواهد بود.
- **DeepSeekMoE رد شد**: `DecoderLayer` فقط یک FFN چگال دارد (بدون
  ساختار متخصص/مسیریاب) و هیچ نقطه بازرسی MoE واقعی وجود ندارد.
- **فشرده‌سازی/رمزگذاری روی GPU**: سود واقعی برای بارهای داده کوچک
  تأیید نشده (سربار میزبان↔دستگاه ممکن است مزیت GPU را خنثی کند).
- **یافته دستگاه واقعی Android (2026-08-15)**: روی یک دستگاه Android
  (moto g53y 5G، Adreno 619)، GPU تلفن هم از CPU و هم از مقایسه
  CPU/GPU دسکتاپ GT730 در اندازه‌های ماتریس بزرگ‌تر (تا حدود ۶ برابر
  در 512×512) پیشی گرفت و فرضیه «GPU تلفن‌ها ممکن است به‌طور شگفت‌آوری
  سریع باشند» را تأیید کرد. نکته احتیاطی: محاسبات کوچک یک‌باره همچنان
  توسط سربار مقداردهی اولیه GPU محدود می‌شوند (۵۸ تا ۶۳ میلی‌ثانیه).

## ماتریس پشتیبانی سازندگان

سه لایه: یکپارچه‌سازی کارکردیِ Vulkan / شمارش `GpuVendor` به‌عنوان
لایه‌ای صرفاً گزارش‌دهنده (شامل Qualcomm/ARM/ImaginationPowerVr
تأییدنشده) / لایه stub کتابخانه‌های سازنده. جزئیات در
`OmniGPU-Design.md` بخش ۸.۵.

## ورودی‌های اخیر مرتبط HANDOFF

- **2026-08-10**: `generate_with_repetition_penalty` (جریمه تکرار به
  سبک CTRL) یک باگ واقعی حلقه بی‌نهایت در رمزگشایی حریصانه GPT-2 در
  `aruaru-llm` را برطرف می‌کند. مقدار پیش‌فرض `penalty=1.3` از طریق
  `ARUARU_LLM_REPETITION_PENALTY`.
- **جدیدترین کار (کامیت `0c43ba3`)**: تابع جدید
  `GptModel::generate_speculative` — رمزگشایی حدسی بدون افت کیفیت به
  سبک DSpark/Leviathan (مدل پیش‌نویس توکن‌ها را پیشنهاد می‌دهد، مدل
  هدف از طریق batch-prefill دسته‌ای تأیید می‌کند). روی داده‌های
  آزمایشی مصنوعی و وزن‌های واقعی به‌صورت بیت‌به‌بیت با `generate()`
  یکسان تأیید شده. **افشای صادقانه**: در مسیر CPU این دستگاه کندتر از
  `generate()` ساده است (draft_k=4: حالت ساده ۴.۶۳ ثانیه در برابر
  حدسی ۷.۶۵ ثانیه)، زیرا GEMM ساده CPU سربار dispatch کمی برای
  استهلاک دارد. سناریوی هدف واقعی — سرعت روی Vulkan واقعی — هنوز
  اندازه‌گیری نشده است.
