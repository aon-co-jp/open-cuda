# open-cuda — دليل النقل (نسخة مختصرة)

> **ملاحظة**: هذه ترجمة مختصرة للأنماط القابلة لإعادة الاستخدام
> حاليًا. يبقى سجل HANDOFF التاريخي المفصّل متاحًا باللغة اليابانية
> فقط في [PORTING.md](PORTING.md) — يجب الرجوع إليه قبل اعتماد أي
> نمط فعليًا.

ملخص أنماط التصميم القابلة لإعادة الاستخدام في هذا المشروع، مفيد عند
نقلها إلى مشروع آخر.

1. **السمة (trait) ‏`GpuDevice` (تصميم مستقل عن الواجهة الخلفية)**:
   عقد أدنى على غرار CUDA Runtime API (‏`alloc`/`free`/
   `memcpy_h2d`/`memcpy_d2h`/`memcpy_d2d`/`launch_kernel`/
   `synchronize`) بالإضافة إلى أعلام القدرات
   (‏`supports_spirv`/`supports_dxil`، الافتراضي `false`). تُنفّذ
   الواجهات الخلفية الجديدة هذا العقد وتوسّع تعداد `KernelSource`
   بطريقة لا تكسر التوافق (مثل `Dxil(Vec<u8>)`).
2. **نمط ثنائي المراحل «محاكاة ← عتاد حقيقي»**: المرحلة 1 (جهاز
   وهمي، يعمل دون عتاد، يتحقق من العقود حتى في بيئات CI بلا GPU)،
   المرحلة 1.5–2 (تنفيذ حقيقي خلف ميزة Cargo، معطّلة افتراضيًا،
   تتجاوز نفسها بصدق عند غياب العتاد الحقيقي).
3. **فخ حشو المصفوفات في cbuffer الخاص بـ HLSL**: تُحشى المصفوفات
   القياسية داخل `cbuffer` إلى حدود 16 بايت — فلا تعود الثوابت
   المكتنزة من جهة Rust/C++ متوافقة مع تخطيط HLSL (سبّب فعليًا خللًا
   «يبقى الناتج نصًّا صريحًا» في نواة ChaCha20). الحل: حقول قياسية
   منفصلة (‏`key0`…`key7`) بدلًا من إعلان مصفوفة.
4. **توقيع الجذر (Root Signature) المضمَّن في HLSL**: السمة
   `[RootSignature(...)]` المكتوبة مباشرة في الشيدر تجعل `dxc` يضمّن
   توقيع الجذر داخل DXIL — ما يوفّر بناء واصف توقيع الجذر يدويًا في
   كود Rust/C++. ربط واصفات UAV الجذرية مباشرة بدلًا من المرور عبر
   descriptor heap.
5. **تعداد محولات DXGI لتحديد المُصنِّع**: `D3D12CreateDevice(None,
   ...)` لا يوفّر معلومات عن المُصنِّع أو سعة VRAM.
   `IDXGIFactory1::EnumAdapters1(0)` ← `DXGI_ADAPTER_DESC1` يوفّر
   معرّف المُصنِّع عبر PCIe (NVIDIA/AMD/Intel). عند الفشل، الرجوع
   بأمان إلى `None`.
6. **تحذير صادق بشأن الضغط/التشفير على GPU**: بالنسبة للحمولات
   الصغيرة (بحجم MTU الشبكي) قد يُلغي عبء نقل Host↔Device الميزة
   الحسابية لوحدة GPU — يجب إجراء قياسات أداء حقيقية على حجم الحمولة
   المستهدف قبل الدمج.
7. **نمط نوى تعادل RAID6**: عدد متغيّر من أقراص البيانات كمخزن
   مؤقت واحد متصل (بدلًا من ربط منفصل لكل قرص). الضرب في GF(2^8)
   لأجل Q-parity (‏Reed-Solomon) كدالة `gf_mul` مستقلة (ضرب على طريقة
   «الفلاح الروسي»، كثيرة حدود غير قابلة للاختزال `0x11D`) داخل
   الشيدر.
8. **تنفيذ على GPU مستقل عن الأعداد الصحيحة 64-بت**: الأعداد
   الصحيحة 64-بت ميزة عتادية اختيارية في DXIL SM6.0. ينفّذ Poly1305
   الضرب/الجمع/الإزاحة الزوجية 32×32←64 بت باستخدام عمليات 32-بت
   فقط — نمط مفيد لنقل التشفير/الأعداد الكبيرة إلى أهداف لا تضمن
   64-بت.
9. **ضغط ذاكرة KV المؤقتة على طراز MLA من DeepSeek-V3**: إسقاط
   تنازلي/تصاعدي مبني على `sgemm` الموجودة. **إفصاح صادق**: مصفوفات
   الإسقاط مهيّأة عشوائيًا فقط (بلا أوزان مدرَّبة) — الضغط فيه فقدان
   للبيانات؛ وهذا يثبت فقط أن المسار الحسابي موصول بشكل صحيح، لا أن
   جودة التوليد محفوظة.
10. **عقوبة التكرار (بأسلوب CTRL)**: `logit>0` ← `/penalty`،
    `logit<=0` ← `*penalty`، تُطبَّق على الرموز التي سبق ظهورها قبل
    argmax. `penalty=1.0` مسار مختصر يحافظ تمامًا على سلوك
    `generate()` الحالي (دون أي تراجع). القيمة الافتراضية التجريبية
    `1.3` — تحتاج لإعادة معايرة حسب بنية الـ prompt.

**الحالة الحالية**: مساحة عمل Cargo مكوّنة من `opencuda-core`/
`opencuda-cpu`/`opencuda-vulkan`/`opencuda-directx`/`opencuda-blas`/
`open-cuda-bert`/`open-cuda-llm`. تم تنفيذ `opencuda-directx` حتى
المرحلة 2 (تم التحقق من vector_add/matmul/ChaCha20/Poly1305 على
عتاد حقيقي). أُضيفت نوى تعادل RAID6 من نوعي P/Q إلى
`opencuda-vulkan` وتم التحقق منها على عتاد حقيقي. التفاصيل في
إدخالات HANDOFF ضمن [CLAUDE.md](CLAUDE.md).
